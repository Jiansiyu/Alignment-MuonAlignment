process TEST = {
  
  service = MessageLogger 
  { 
	untracked vstring destinations = { "info_txt", "cerr" }
	untracked PSet info_txt  = { untracked string threshold = "INFO" }
	untracked PSet cerr      = { untracked string threshold = "ERROR" }
	
  }

  untracked PSet maxEvents = { untracked int32 input = 1 }
  source = EmptySource { }

  include "Geometry/MuonCommonData/data/muonIdealGeometryXML.cfi"
  include "Geometry/MuonNumbering/data/muonNumberingInitialization.cfi"

## DT geometry 
#  include "Geometry/DTGeometry/data/dtGeometry.cfi"
## CSC geometry
#   include "Geometry/CSCGeometry/data/cscGeometry.cfi"


# Misalignment example scenario producer
  include "Alignment/MuonAlignment/data/MisalignedMuon.cfi"
  es_module MisalignedTracker = MisalignedTrackerESProducer
 {
     using MuonNoMovementsScenario

#   using MuonSurveyOnlyScenario
#
#   using Muon100inversePbScenario2008
#   using Muon10inversePbScenario2008
#   using Muon0inversePbScenario2008
#
#   using Muon100InversepbScenario
#   using Muon10InversepbScenario
 }

  # Test module
  #module prod =  TestMuonAnalyzer { untracked string fileName = "aligned.root" }
  module prod =  TestRotation { untracked string fileName = "misaligned-2.root" }
  
  path p1 = { prod }      
  
}
