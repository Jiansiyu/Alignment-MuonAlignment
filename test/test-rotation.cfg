process TEST = {
  
  service = MessageLogger 
  { 
	untracked vstring destinations = { "info.txt", "cerr" }
	untracked PSet info.txt  = { untracked string threshold = "INFO" }
	untracked PSet cerr      = { untracked string threshold = "ERROR" }
	
  }

  source = EmptySource 
  { 
	untracked int32 maxEvents = 1 
  }

# DT geometry 
  include "Geometry/MuonCommonData/data/muonIdealGeometryXML.cfi"
  include "Geometry/DTGeometry/data/dtGeometry.cfi"

# CSC geometry
#   include "Geometry/MuonCommonData/data/muonEndcapIdealGeometryXML.cfi"
#   include "Geometry/CSCGeometry/data/cscGeometry.cfi"



# Misalignment example scenario producer
  #include "Alignment/MuonAlignment/data/NoMovementsScenario.cfi"
  es_module MisalignedTracker = MisalignedTrackerESProducer
 {
  
       // -- Top-level --//
       string distribution = 'fixed'
       int32 seed = 1234567
  
 }

  # Test module
  #module prod =  TestAnalyzer { untracked string fileName = "aligned.root" }
  module prod =  TestRotation { untracked string fileName = "misaligned-2.root" }
  
  path p1 = { prod }      
  
  module print = AsciiOutputModule {}

  endpath ep = { print }

}
